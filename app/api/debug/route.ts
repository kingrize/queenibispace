import { NextResponse } from 'next/server'; import { collection, getDocs, query, where } from 'firebase/firestore'; import { db } from '@/lib/firebase'; export async function GET(req) { const { searchParams } = new URL(req.url); const uid = searchParams.get('uid'); try { const q = uid ? query(collection(db, 'storage_files'), where('uid', '==', uid)) : query(collection(db, 'storage_files')); const snapshot = await getDocs(q); const docs = []; snapshot.forEach(doc => docs.push({id: doc.id, ...doc.data()})); return NextResponse.json({ count: docs.length, docs }); } catch (e) { return NextResponse.json({ error: e.message, code: e.code }, { status: 500 }); } }
